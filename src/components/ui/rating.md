# Rating Component

Кастомный компонент для отображения и выбора рейтинга в виде звёзд.

## Возможности

- ⭐ Полные и половинчатые звёзды
- 🎨 Три размера: `sm`, `md`, `lg`
- 🔒 Режим только для чтения (readonly)
- 🖱️ Интерактивный выбор рейтинга
- 🔢 Отображение числового значения
- 👥 Отображение количества отзывов
- ⌨️ Поддержка клавиатуры
- ♿ Accessibility (ARIA attributes)

## Использование

### Базовое использование

```tsx
import { Rating } from '@/components/ui';

// Readonly рейтинг (только отображение)
<Rating value={4.5} readonly />

// Интерактивный рейтинг
<Rating 
  value={rating} 
  onChange={(newValue) => setRating(newValue)} 
/>
```

### С числовым значением и количеством отзывов

```tsx
<Rating 
  value={4.5} 
  readonly 
  showValue 
  count={1234} 
/>
// Отобразит: ⭐⭐⭐⭐☆ 4.5 (1,234)
```

### Разные размеры

```tsx
<Rating size="sm" value={4} />   // Маленький
<Rating size="md" value={4} />   // Средний (по умолчанию)
<Rating size="lg" value={4} />   // Большой
```

### Без половинчатых звёзд

```tsx
<Rating 
  value={3.7} 
  allowHalf={false} 
  onChange={setRating}
/>
// Округлит до ближайшего целого
```

### Настройка максимального рейтинга

```tsx
<Rating 
  value={8} 
  max={10} 
  onChange={setRating}
/>
// 10 звёзд вместо 5
```

## Props

| Prop | Type | Default | Описание |
|------|------|---------|----------|
| `value` | `number` | - | Текущее значение рейтинга (обязательно) |
| `max` | `number` | `5` | Максимальное значение рейтинга |
| `readonly` | `boolean` | `false` | Режим только для чтения |
| `onChange` | `(value: number) => void` | - | Callback при изменении значения |
| `size` | `'sm' \| 'md' \| 'lg'` | `'md'` | Размер звёзд |
| `allowHalf` | `boolean` | `true` | Разрешить половинчатые звёзды |
| `showValue` | `boolean` | `false` | Показывать числовое значение |
| `count` | `number` | - | Количество отзывов (опционально) |
| `className` | `string` | - | Дополнительные CSS классы |

## Примеры использования в проекте

### В карточке места (PlaceCard)

```tsx
<Card>
  <CardHeader>
    <CardTitle>{place.name}</CardTitle>
  </CardHeader>
  <CardContent>
    <Rating 
      value={place.rating} 
      readonly 
      showValue 
      count={place.reviewsCount}
      size="sm"
    />
  </CardContent>
</Card>
```

### В форме создания отзыва

```tsx
const [rating, setRating] = useState(0);

<form>
  <Label>{t('review.yourRating')}</Label>
  <Rating 
    value={rating}
    onChange={setRating}
    size="lg"
  />
</form>
```

### В детальной информации о месте

```tsx
<div>
  <h2>{place.name}</h2>
  <Rating 
    value={place.averageRating} 
    readonly 
    showValue 
    count={place.totalReviews}
  />
</div>
```

## Стилизация

Компонент использует цвета из design system:
- Заполненные звёзды: `text-amber-400 fill-amber-400`
- Пустые звёзды: `text-muted-foreground/20` (readonly: `/30`)

Можно кастомизировать через className:

```tsx
<Rating 
  value={5} 
  className="gap-2" 
/>
```

## Accessibility

- Использует `role="radiogroup"` для группы звёзд
- Каждая звезда имеет `role="radio"` и `aria-checked`
- Поддерживает навигацию клавиатурой (Enter, Space)
- ARIA label для screen readers

## Технические детали

- Построен на `lucide-react` иконке `Star`
- Использует controlled component pattern
- Hover эффект для предпросмотра при выборе
- Плавные CSS transitions
- Не зависит от внешних библиотек (кроме lucide-react)
